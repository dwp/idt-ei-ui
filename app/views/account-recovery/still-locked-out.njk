{% extends "../layouts/one-column.njk" %}

{% block pageTitle %}
    {{ t('account-recovery-still-locked-out:title') }}
{% endblock %}

{% block mainContent %}
    <h1 class="govuk-heading-l" id="page-heading">
        {{ t('account-recovery-still-locked-out:h1') }}
    </h1>
    <p>{{ t('account-recovery-still-locked-out:p-1') }}</p>
    <p>{{ t('account-recovery-still-locked-out:p-2') }}<a id="account-recovery" href="{{ accountRecoveryLink }}">{{ t('account-recovery-still-locked-out:account-recovery-link') }}</a> {{ t('account-recovery-still-locked-out:p-3') }}</p>
{% endblock %}