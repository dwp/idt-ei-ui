{% extends "../layouts/one-column.njk" %}

{% block pageTitle %}
    {{ t('auth-still-locked-out:title') }}
{% endblock %}

{% block mainContent %}
    <h1 class="govuk-heading-l" id="page-heading">{{ t('auth-still-locked-out:h1') }}</h1>
    <p>{{ t('auth-still-locked-out:p-1') }}</p>
    <p>{{ t('auth-still-locked-out:p-2') }}<a id = "sign-in" href="{{ signinLink }}">{{ t('auth-still-locked-out:sign-inLink') }}</a> {{ t('auth-still-locked-out:p-3') }}</p>
{% endblock %}
